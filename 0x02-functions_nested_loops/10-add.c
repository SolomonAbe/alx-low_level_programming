#include "main.h"

/**
 * add - adds two integers and returns the result
 * @i & @j - values to be sent for calculation
 * return sum of i & j
 */
int add(int i, int j)
{
	int result;
	
	result = i + j;
	return (result);
}
