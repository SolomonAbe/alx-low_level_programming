#include "main.h"

/**
 * add - adds two integers and returns the result
 * @i - value one
 * @j - valie two
 * Return - sum of i & j
 */
int add(int i, int j)
{
	int result;

	result = i + j;
	return (result);
}
