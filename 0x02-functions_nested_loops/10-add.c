#include "main.h"

/**
 * add - calcualte sum of two integers i and j then returns the result
 * @i: value one
 * @j: value two
 * Return: sum of i & j
 */
int add(int i, int j)
{
	int result;

	result = i + j;
	return (result);
}
